from django.apps import AppConfig


class InvestigationOsintConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "investigation_osint"
    verbose_name = "Investigation (Multi-Vector Correlation)"
